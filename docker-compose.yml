services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: flags_train
      POSTGRES_USER: flags
      POSTGRES_PASSWORD: flags
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U flags -d flags_train"]
      interval: 10s
      timeout: 5s
      retries: 5

  data:
    build:
      context: .
    profiles: ["setup"]
    command: sh -c "python scripts/fetch_countries_data.py && python scripts/validate_countries_data.py"
    volumes:
      - ./data:/app/data

  bot:
    build:
      context: .
    restart: unless-stopped
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy
    command: sh -c "python scripts/validate_countries_data.py && alembic upgrade head && python scripts/runtime_preflight.py && python scripts/run_bot.py"
    volumes:
      - ./data:/app/data

volumes:
  postgres_data:
